# Minimal, pinned environment for the nested-MRIOT reconstruction routine.
# Versions are those of the source environment (Python 3.9). Install with:
#     python -m venv .venv && source .venv/bin/activate
#     pip install -r requirements.txt && pip install -e .
#
# NOTE: reading the WiNDC .gdx (step 10/11) also needs a licensed GAMS install;
#       the gamsapi / gamspy_base packages below are only the Python bindings.

# --- core numerics / IO ---
numpy==2.0.2
pandas==2.2.3
scipy==1.13.1
pyarrow==21.0.0
fastparquet==2024.11.0
openpyxl==3.1.5          # read the BEA .xlsx concordance
tqdm==4.68.3

# --- WiNDC GDX reader (needs a licensed GAMS install alongside) ---
GDX2py==2.2.0
gamsapi==47.6.0
gamspy_base==53.4.0

# --- figures / validation ---
matplotlib==3.9.4
seaborn==0.13.2
networkx==3.2.1          # network statistics of the delivered table (Data Overview)
requests==2.32.5         # figures/fig08_distance_variants.py fetches its Census inputs

# --- notebook execution (run_all.sh) ---
ipykernel==6.31.0
nbconvert==7.17.1
nbformat==5.10.4
nbclient==0.10.2
jupyter_client==8.6.3
